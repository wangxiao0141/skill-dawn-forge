# 秘密不得进入装机 profile 和仓库

装机需要订阅地址、密码、token 或私钥时，内容必须由用户提供，不在装机 profile、仓库、输出或运行状态中保存。订阅地址等需要交给目标机应用的配置，由 Agent 通过管理连接写成配置文件或传输用户提供的文件，再由用户手动应用；密码、token 和私钥仍由用户在目标机本地输入。这样既避免 Agent 猜测或持久化秘密，也不要求用户在目标机重新寻找已有配置。
